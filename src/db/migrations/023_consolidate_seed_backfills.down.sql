-- No-op rollback for 023_consolidate_seed_backfills.

-- These statements consolidate idempotent compatibility guards and legacy data
-- backfills that are not safely reversible without losing user configuration.
